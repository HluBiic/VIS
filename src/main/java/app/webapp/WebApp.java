package app.webapp;

/**
 * This main method in this class is NOT executed by the servlet container, because
 * in this project servlet container Tomcat is used. Tomcat automatically detects
 * classes with the (@WebServlet) annotations.
 */
public class WebApp {
	/**
	 * Main method (placeholder). This method is ignored when the application
	 * is deployed on a servlet container (Tomcat).
	 */
	public static void main(String[] args) {
		//homepage: http://localhost:8080/r6matches/index
	}
}